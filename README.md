Using Deno Kv for the database, it uses foundationDB without configuring anything.
For Node.js I'm using Hono

how to run the project
```bash
deno run --unstable --allow-net --allow-read --watch app.ts 
```